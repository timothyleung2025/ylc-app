import { ArrowUpRight, Clock3, MapPin } from "lucide-react";
import Link from "next/link";
import { Pill } from "./ui";
export function ActivityCard({current=false,title,time,location,href="/schedule"}:{current?:boolean;title:string;time:string;location:string;href?:string}){return <div className={`card relative overflow-hidden p-5 ${current?"bg-[var(--charcoal-blue)] text-white":""}`}>{current&&<div className="absolute -right-8 -top-12 size-36 rounded-full bg-[var(--strong-cyan)]/20"/>}<div className="mb-4 flex items-center justify-between"><Pill color={current?"#60C9C5":"#DDF3F2"}>{current?"HAPPENING NOW":"UP NEXT"}</Pill></div><h3 className="font-display text-3xl">{title}</h3><div className={`mt-4 space-y-2 text-sm ${current?"text-white/75":"text-[var(--muted)]"}`}><p className="flex items-center gap-2"><Clock3 size={16}/>{time}</p><p className="flex items-center gap-2"><MapPin size={16}/>{location}</p></div>{current&&<Link href={href} className="mt-5 flex items-center justify-between border-t border-white/15 pt-4 text-sm font-bold">View session details <ArrowUpRight size={18}/></Link>}</div>}
