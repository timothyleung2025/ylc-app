import { Team } from "@/lib/types";
export function LeaderboardRow({team,highlight=false}:{team:Team;highlight?:boolean}){return <div className={`flex items-center gap-3 rounded-2xl p-3 ${highlight?"bg-[var(--strong-cyan)]/20":""}`}><span className={`grid size-8 place-items-center rounded-full text-xs font-black ${team.rank===1?"bg-[var(--strong-cyan)]":"bg-[var(--charcoal-blue)]/6"}`}>{team.rank}</span><span className="size-3 rounded-full" style={{background:team.color}}/><span className="flex-1 font-bold">{team.name}</span><span className="font-display text-2xl">{team.points}</span><span className="text-[10px] font-bold text-[var(--muted)]">PTS</span></div>}
