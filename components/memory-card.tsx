"use client";
import { Heart } from "lucide-react";
import { useState } from "react";
import { MemoryPost } from "@/lib/types";
export function MemoryCard({post}:{post:MemoryPost}){const [liked,setLiked]=useState(false);return <article className={`break-inside-avoid ${post.style==="polaroid"?"rotate-[1deg] bg-white p-3 pb-5":post.style==="postcard"?"rotate-[-1deg] border-2 border-dashed border-[#b88f4d] p-5":"rounded-sm p-5"} mb-4 shadow-md`} style={{backgroundColor:post.style==="polaroid"?"white":post.color}}>{post.image&&<div className="mb-4 aspect-[4/3] overflow-hidden bg-stone-200"><img src={post.image} alt="Friends at YLC" className="h-full w-full object-cover"/></div>}<div className="mb-3 flex items-center gap-2"><span className="grid size-8 place-items-center rounded-full bg-[var(--forest)] text-[10px] font-bold text-white">{post.initials}</span><div><p className="text-xs font-bold">{post.author} · {post.team}</p><p className="text-[10px] font-bold uppercase tracking-wide text-[var(--muted)]">{post.type} · {post.time}</p></div></div><p className="font-display text-lg font-semibold leading-snug">{post.text}</p><button onClick={()=>setLiked(!liked)} aria-label="Like memory" className={`mt-4 flex items-center gap-1 text-xs font-bold ${liked?"text-[var(--red)]":"text-[var(--muted)]"}`}><Heart size={16} fill={liked?"currentColor":"none"}/>{post.likes+(liked?1:0)}</button></article>}
