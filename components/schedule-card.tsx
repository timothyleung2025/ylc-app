import { Check, Clock3, MapPin, Radio } from "lucide-react";
import { ScheduleEvent } from "@/lib/types";
const tagColors:Record<ScheduleEvent["type"],string>={Session:"#DDF3F2",Workshop:"#D7E8E8","Team Challenge":"#A9DFDC",Meal:"#F2F2F2",Break:"#FFF0C8",Presentation:"#DCE7EA"};
export function ScheduleCard({event,onClick}:{event:ScheduleEvent;onClick:()=>void}){return <button onClick={onClick} className={`card flex w-full gap-4 p-4 text-left transition hover:-translate-y-0.5 ${event.status==="now"?"ring-2 ring-[var(--green)] ring-offset-2 ring-offset-[var(--cream)]":""}`}><div className="w-17 shrink-0 pt-1"><p className="text-sm font-extrabold text-[var(--forest)]">{event.time.replace(" "," ")}</p>{event.endTime&&<p className="mt-1 text-xs text-[var(--muted)]">{event.endTime}</p>}</div><div className="min-w-0 flex-1 border-l border-green-900/10 pl-4"><div className="mb-2 flex flex-wrap items-center gap-2"><span className="rounded-full px-2.5 py-1 text-[10px] font-extrabold" style={{background:tagColors[event.type]}}>{event.type}</span>{event.status==="now"&&<span className="flex items-center gap-1 text-[10px] font-extrabold text-[var(--red)]"><Radio size={11}/>LIVE</span>}{event.status==="completed"&&<Check size={14} className="text-[var(--green)]"/>}</div><h3 className={`font-display text-lg font-bold leading-tight ${event.status==="completed"?"text-stone-500":""}`}>{event.title}</h3><p className="mt-2 flex items-center gap-1.5 text-xs text-[var(--muted)]"><MapPin size={13}/>{event.location}</p></div></button>}
